{
  "name": "glyph-crypt",
  "version": "0.5.1",
  "description": "Glyph Crypt — a neon slide-maze arcade game (browser, TypeScript).",
  "type": "module",
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "generate-pwa-assets": "pwa-assets-generator"
  },
  "devDependencies": {
    "@vite-pwa/assets-generator": "^1.0.2",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^2.1.4"
  }
}
